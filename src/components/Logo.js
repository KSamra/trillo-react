import styled from 'styled-components';
import logo from '../img/logo.png';

const Logo = styled.img.attrs({
  src: logo
})`
  height: 3.25rem;
  margin-right: 3rem;
  padding-left: 1rem;

`;

export default Logo;